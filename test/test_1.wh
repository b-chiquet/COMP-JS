function exampleFunction :
  read A
   %
if A
then nop
fi
%
write B


function secondExampleFunction :
  read A
   %
if not A
then nop
fi;

foreach A=?B in (A and B or C) do nop;nop   od;

TestVariable := 12
;
AutreVar := TestVariable

%
write B

